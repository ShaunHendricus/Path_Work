services:
  ros_bridge:
    build:
      context: .
      dockerfile: Dockerfile
    image: ros_bridge:latest
    network_mode: host
    environment:
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
    stdin_open: true
    tty: true